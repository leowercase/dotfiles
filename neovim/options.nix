_: {
  config = {
    luaLoader.enable = true;

    clipboard.register = "unnamedplus";

    options = {
      number = true;
      relativenumber = true;
      
      termguicolors = true;

      # Tabs are 4 spaces long by default
      shiftwidth = 4;
      tabstop = 4;
    };
  };
}
